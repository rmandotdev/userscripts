{
  "name": "userscripts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run ./src/build/build.ts",
    "deploy": "bunx wrangler deploy"
  },
  "dependencies": {
    "infinibrowser": "^0.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.20",
    "@violentmonkey/types": "^0.2.2",
    "infinite-craft": "^0.0.6",
    "wrangler": "^4.30.0"
  },
  "peerDependencies": {
    "typescript": "5.9.2"
  }
}
