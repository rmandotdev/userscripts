{
  "name": "userscripts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run ./src/build/build.ts",
    "deploy": "bunx wrangler deploy"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@violentmonkey/types": "^0.2.2",
    "wrangler": "^4.21.1"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  }
}
