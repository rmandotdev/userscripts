import { defineConfig } from "#config";

export default defineConfig({
  headers: {
    name: "KIT v4",
    match: "https://neal.fun/infinite-craft/",
    author: "Catstone + GameRoMan",
    namespace: "Catstone",
    version: "4.0.1",
    description:
      "Kit can literally make anything, apart from some stuff. Open the Console (Ctrl + Shift + I) and type revive(`words`) in there. Seperate multiple elements by new lines. To modify tools used, check out the settings at the top of the code; Patched by GameRoMan",
  },
});
