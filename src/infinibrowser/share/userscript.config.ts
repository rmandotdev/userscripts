import { defineConfig } from "build/config";

export default defineConfig({
  headers: {
    name: "Share Optimal Lineages",
    match: "https://infinibrowser.wiki/analyzer*",
    version: "1.0",
    description: "Share Optimal Lineages from Analyser",
  },
});
